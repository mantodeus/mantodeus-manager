# ============================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================
# This file is for LOCAL/STAGING development only.
# Copy this file to .env.local and fill in your values.
# 
# IMPORTANT: 
# - .env.local is gitignored and will NEVER be committed
# - Use staging/test credentials here, NOT production
# - Production uses .env (which is also gitignored)
# ============================================

# ============================================
# DATABASE (STAGING/LOCAL)
# ============================================
# Use a local MySQL instance or staging database
DATABASE_URL=mysql://username:password@localhost:3306/mantodeus_manager_staging

# ============================================
# JWT SECRET (LOCAL/STAGING)
# ============================================
# Generate a random string for local development
# This should be DIFFERENT from production
JWT_SECRET=local_dev_secret_key_change_me_in_production

# ============================================
# OAUTH CONFIGURATION (STAGING/LOCAL)
# ============================================
# Use staging OAuth endpoints if available, or production endpoints for testing
OAUTH_SERVER_URL=https://api.manus.im
VITE_OAUTH_PORTAL_URL=https://portal.manus.im
VITE_APP_ID=your_staging_app_id_here

# ============================================
# APPLICATION CONFIGURATION
# ============================================
VITE_APP_TITLE=Mantodeus Manager (Local)
VITE_APP_LOGO=/mantodeus-logo.png
PORT=3000
NODE_ENV=development

# ============================================
# OWNER CONFIGURATION (OPTIONAL)
# ============================================
OWNER_OPEN_ID=your_owner_open_id
OWNER_NAME=Your Name

# ============================================
# S3 STORAGE (STAGING/LOCAL BUCKET)
# ============================================
# Use a STAGING bucket for local development
# NEVER use production bucket credentials here
S3_ENDPOINT=https://s3.pub1.infomaniak.cloud
S3_REGION=us-east-1
S3_BUCKET=mantodeus-manager-files-staging
S3_ACCESS_KEY_ID=your_staging_access_key
S3_SECRET_ACCESS_KEY=your_staging_secret_key

# ============================================
# SUPABASE (IF USED)
# ============================================
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
OWNER_SUPABASE_ID=your_owner_supabase_id

# ============================================
# NOTES
# ============================================
# - All values above are examples - replace with your actual staging/local values
# - Never commit .env.local to git (it's already in .gitignore)
# - Use different credentials for staging vs production
# - For production deployment, use .env file (also gitignored)
